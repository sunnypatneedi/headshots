"""headshots: frame, finish and check a folder of portraits, then group them by who is in them.

Everything runs on the machine it is installed on. On the free path nothing is uploaded, and after
the two face models are fetched once, nothing needs the network again. Premium can ask a remote
model (`--decide jev`) about each photo's grade, reasons, and measurements. The photo stays here.
"""
__version__ = "0.1.0"
