"""headshots: frame, finish and check a folder of portraits, then group them by who is in them.

Everything runs on the machine it is installed on. Nothing is uploaded, and after the two face
models are fetched once, nothing needs the network again.
"""
__version__ = "0.1.0"
