# headshots architecture

Standalone Archify diagram explaining the system to non-technical and technical readers.

| File | Role |
| --- | --- |
| `headshots.html` | Interactive viewer (theme, views, export) |
| `headshots.architecture.json` | Source IR — re-deliver with Archify after edits |

Views in the HTML: **Plain story**, **Technical stack**, **Stays on device**.

Cards under the diagram summarize the same split: for everyone / for engineers / trust boundary.
